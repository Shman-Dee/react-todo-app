import Counter from "./Counter";
// this is a functional component

//returns html

//you can think of components as partials

const App = () => {
  return (
    <div>
      <Counter />
      <Counter />
      <Counter />
    </div>
  );
};

export default App;
